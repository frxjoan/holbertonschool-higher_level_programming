# Python - Test Driven Development

This project contains Python functions developed using Test Driven Development (TDD).
Each module includes documentation and is validated using doctest test cases stored in the `tests` directory.

## Requirements
- Ubuntu 20.04 LTS
- Python 3.8.5
- pycodestyle 2.7.*
- Tests: `python3 -m doctest ./tests/*`
